# `--version` flag — EARS spec

Ticket: `issuedb` #5 (in-progress)
Source: `PORTABILITY-FREEBSD.md` §5

## Requirements

- When the user runs `supervice --version` or `supervicectl --version`, the
  system shall print the package version and exit 0.
